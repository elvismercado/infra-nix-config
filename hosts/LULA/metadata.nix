# Cross-host metadata for LULA (2026 MacBook Neo, Apple A-series).
#
# Maria's personal laptop — a non-technical user's machine. No syncthing
# block (LULA is not part of the syncthing mesh) and no LAN IP (laptop
# roams between networks; reachable via global discovery if ever added
# to syncthing later).
{
  hostname = "LULA";
  managed = true;
  os = "darwin";
  lanIp = null;
}
